# Google Sheets API 연동
# 1. Google Cloud Console에서 Sheets API 활성화
# 2. 서비스 계정 생성 후 JSON 키 다운로드
# 3. credentials.json을 server/ 폴더에 저장
# 4. Google 시트를 서비스 계정 이메일에 '편집자' 권한 부여
#    ※ credentials.json의 client_email과 동일 (Google Cloud에 등록된 계정명)

GOOGLE_SPREADSHEET_ID=1mTPejSsuMKuNMfdzAc2N-4p8HFRMwSIY9RQkOGCxUoY
GOOGLE_CREDENTIALS_PATH=./credentials.json
GOOGLE_SHEET_NAME=Sheet1

# 서버 포트
PORT=3001

# 이메일 전송 (Gmail 예시)
# Gmail: 앱 비밀번호 생성 필요 (Google 계정 → 보안 → 2단계 인증 → 앱 비밀번호)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
